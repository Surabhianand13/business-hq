{
  "name": "krispies-dispatch",
  "version": "1.0.0",
  "scripts": {
    "build": "cd frontend && npm install && npm run build && cp -r dist ../backend/public",
    "start": "cd backend && node index.js",
    "install-deps": "cd backend && npm install"
  }
}
